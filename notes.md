
python -m venv ~/myvenv # to create a virtual environment called my _myvenv_

source ~/myvenv/bin/activate # to activate

python -m reloadserver
tailwindcss -i css/input.css -o css/styles.css --watch
